name: Qualify v0.33 The Split

on:
  push:
    branches:
      - feat/v0.33-the-split

permissions:
  contents: write

env:
  CI: 'true'

jobs:
  qualify:
    if: ${{ !contains(github.event.head_commit.message, '[freeze-v033]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check zopfli==0.4.3
      - run: npm run release:competition
      - run: |
          wc -c dist/index.html dist/stretchicorn-js13k.zip
          sha256sum dist/stretchicorn-js13k.zip

  browser-smoke:
    name: Browser smoke (${{ matrix.browser }})
    needs: qualify
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check zopfli==0.4.3
      - run: npm run release:competition
      - run: |
          rm -rf .tmp-js13k
          mkdir .tmp-js13k
          python -m zipfile -e dist/stretchicorn-js13k.zip .tmp-js13k
      - run: npm install --no-save --package-lock=false --ignore-scripts --no-audit --no-fund playwright@1.55.0
      - run: npx playwright install --with-deps ${{ matrix.browser }}
      - env:
          BROWSER: ${{ matrix.browser }}
          BROWSER_HTML: .tmp-js13k/index.html
        run: npm run browser:smoke
      - env:
          BROWSER: ${{ matrix.browser }}
          BROWSER_HTML: dist/stretchicorn-local.html
        run: npm run browser:file-smoke

  freeze:
    needs: [qualify, browser-smoke]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check zopfli==0.4.3
      - run: npm run release:competition
      - shell: bash
        run: |
          rm -f dist/stretchicorn-desktop-v0.32.0.zip
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add dist
          if ! git diff --cached --quiet; then
            git commit -m "Freeze qualified v0.33 artifacts [freeze-v033]"
            git push
          fi
      - run: |
          wc -c dist/stretchicorn-js13k.zip
          sha256sum dist/stretchicorn-js13k.zip
