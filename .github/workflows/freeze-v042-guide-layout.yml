name: Freeze v0.42 guide layout artifacts
on:
  push:
    branches: [feat/v042-popcorn-finale-style-summary]
    paths: ['.github/workflows/freeze-v042-guide-layout.yml']
permissions:
  contents: write
jobs:
  freeze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with: {ref: feat/v042-popcorn-finale-style-summary}
      - uses: actions/setup-node@v5
        with: {node-version: 22}
      - uses: actions/setup-python@v6
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check zopfli==0.4.3
      - run: npm run release:competition
      - name: Commit exact generated artifacts
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add dist
          git diff --cached --quiet && exit 0
          git commit -m 'build: freeze compact Field Guide artifacts'
          git push origin HEAD:feat/v042-popcorn-finale-style-summary
