import {createServer} from 'node:http';
import {readFile} from 'node:fs/promises';
const engine=process.env.BROWSER||process.argv[2]||'chromium',artifact=process.env.BROWSER_HTML||'dist/index.html';let playwright;try{playwright=await import('playwright')}catch{console.error('Playwright is required. Install playwright@1.55.0');process.exit(2)}const launcher=playwright[engine];if(!launcher||typeof launcher.launch!='function')throw Error(`Unsupported browser engine: ${engine}`);
const html=await readFile(artifact),server=createServer((req,res)=>{if(req.url==='/'||req.url==='/index.html'){res.writeHead(200,{'content-type':'text/html; charset=utf-8','cache-control':'no-store'});res.end(html)}else if(req.url==='/favicon.ico'){res.writeHead(204);res.end()}else{res.writeHead(404);res.end('not found')}});await new Promise((ok,no)=>{server.once('error',no);server.listen(0,'127.0.0.1',ok)});const origin=`http://127.0.0.1:${server.address().port}`,external=[],pageErrors=[],consoleErrors=[];let browser;
try{browser=await launcher.launch({headless:true});const context=await browser.newContext({viewport:{width:1280,height:800}}),page=await context.newPage();await page.route('**/*',async route=>{let u=new URL(route.request().url());if(u.origin===origin)return route.continue();external.push(u.href);return route.abort('blockedbyclient')});page.on('pageerror',e=>pageErrors.push(e.message));page.on('console',m=>{if(m.type()==='error'&&!/favicon/i.test(m.text()))consoleErrors.push(m.text())});await page.goto(`${origin}/`,{waitUntil:'load',timeout:15000});const canvas=page.locator('#c');await canvas.waitFor({state:'visible',timeout:5000});const g=await canvas.evaluate(n=>({width:n.width,height:n.height,cssWidth:n.getBoundingClientRect().width,cssHeight:n.getBoundingClientRect().height}));if(g.width!==960||g.height!==640||g.cssWidth<=0||g.cssHeight<=0)throw Error(`bad canvas geometry: ${JSON.stringify(g)}`);await page.waitForTimeout(120);
let intro=await canvas.evaluate(n=>n.toDataURL());await page.keyboard.press('Space');await page.waitForTimeout(120);let practice=await canvas.evaluate(n=>n.toDataURL());if(intro===practice)throw Error('story did not visibly enter First Flight');await page.keyboard.down('a');await page.waitForTimeout(420);await page.keyboard.up('a');await page.keyboard.down('ArrowRight');await page.waitForTimeout(60);await page.keyboard.up('ArrowRight');for(let i=0;i<3;i++){await page.keyboard.down('a');await page.waitForTimeout(360);await page.keyboard.up('a');await page.keyboard.press('Space');await page.waitForTimeout(230)}await page.waitForTimeout(850);let play=await canvas.evaluate(n=>n.toDataURL());if(practice===play)throw Error('three real First Flight Snaps did not enter Easy');await page.keyboard.press('p');await page.waitForTimeout(80);let paused=await canvas.evaluate(n=>n.toDataURL());if(play===paused)throw Error('Easy gameplay did not pause');await page.keyboard.press('p');await page.waitForTimeout(50);await page.keyboard.press('m');await page.waitForTimeout(80);let menu=await canvas.evaluate(n=>n.toDataURL());await page.keyboard.press('2');await page.waitForTimeout(180);let normal=await canvas.evaluate(n=>n.toDataURL());if(menu===normal)throw Error('manual Normal selection did not start gameplay');if(external.length)throw Error(`external requests attempted: ${[...new Set(external)].join(', ')}`);if(pageErrors.length)throw Error(`page errors: ${pageErrors.join(' | ')}`);if(consoleErrors.length)throw Error(`console errors: ${consoleErrors.join(' | ')}`);console.log(`PASS: ${engine} loaded ${artifact}, completed three real First Flight Snaps, entered Easy, and attempted no external requests`);await context.close()}finally{if(browser)await browser.close();await new Promise(ok=>server.close(ok))}
