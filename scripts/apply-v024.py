from pathlib import Path
import json,re


def subf(s,name,next_name,new):
    a=s.find('function '+name+'(')
    b=s.find('\nfunction '+next_name+'(',a)
    if a<0 or b<0: raise SystemExit('replace failed '+name)
    return s[:a]+new+s[b:]

p=Path('package.json');j=json.loads(p.read_text());j['version']='0.24.0';p.write_text(json.dumps(j,indent=2)+'\n')
p=Path('index.html');s=p.read_text();s=re.sub(r'<title>.*?</title>','<title>Stretchicorn v0.24.0 - LUMINOUS WORLD</title>',s);p.write_text(s)

p=Path('src/03-render.js');s=p.read_text()
s=s.replace("function bar(x,y,w,v,c){X.fillStyle='#10221c';X.fillRect(x,y,w,9);X.fillStyle=c;X.fillRect(x,y,w*cl(v,0,1),9)}\nfunction txt(s,x,y,n=14,c='#fff',a='left'){X.font=(n>18?'bold ':'')+n+'px monospace';X.fillStyle=c;X.textAlign=a;X.fillText(s,x,y)}",
"function bar(x,y,w,v,c){let q=w*cl(v,0,1);X.fillStyle='#09120f';X.fillRect(x,y,w,9);X.fillStyle=c;X.fillRect(x,y,q,9);X.globalAlpha=.3;X.fillStyle='#fff';X.fillRect(x+1,y+1,q-2,2);X.globalAlpha=1}\nfunction txt(s,x,y,n=14,c='#fff',a='left'){X.font=(n>18?'700 ':'500 ')+n+'px system-ui';X.fillStyle=c;X.textAlign=a;X.fillText(s,x,y)}\nfunction oval(x,y,a,b,c){X.fillStyle=c;X.beginPath();X.ellipse(x,y,a,b,0,0,T);X.fill()}\nfunction ink(c,w){X.strokeStyle=c;X.lineWidth=w}")

body="""function body(){let dx=P.x-A.x,dy=P.y-A.y,d=ln(dx,dy),ux=dx/d,uy=dy/d,px=-uy,py=ux,n=6+~~(charge*4),pts=[[A.x,A.y]],fr=cl((5-hearts)/5,0,1),amp=1.5+charge*3.5;for(let i=1;i<n;i++){let q=i/n,w=Math.sin(q*Math.PI*3+t*4)*amp+(rn(2)-1)*fr*9;pts.push([A.x+dx*q+px*w+(rn(2)-1)*fr*4,A.y+dy*q+py*w+(rn(2)-1)*fr*4])}pts.push([P.x,P.y]);X.lineCap='round';let life=1-fr*(.18+(Math.sin(t*37)>.6?.18:0));X.globalAlpha=(wave==1?cl(runT/1.3,0,1):1)*life*(mode==5?cl((winT-.45)/2.1,0,1):1);X.shadowColor=RC[~~(t*4)%6];X.shadowBlur=7+charge*15;ink(inv?'#fff':'#f8f2ff',30);X.beginPath();X.moveTo(pts[0][0],pts[0][1]);for(let i=1;i<pts.length;i++)X.lineTo(pts[i][0],pts[i][1]);X.stroke();X.shadowBlur=0;let off=[-10,-6,-2,2,6,10];for(let i=0;i<6;i++){ink(RC[i],5.1-fr*1.5);X.beginPath();X.moveTo(pts[0][0]+px*off[i],pts[0][1]+py*off[i]);for(let j=1;j<pts.length;j++)X.lineTo(pts[j][0]+px*off[i],pts[j][1]+py*off[i]);X.stroke()}X.globalAlpha=1;X.lineCap='butt'}"""
uni="""function uni(){let a=fa(),e=mode==5?cl((3-winT)/2.3,0,1):0,g=ready>0||kick>0;body();X.save();X.translate(A.x-e*34,A.y+(H-12-A.y)*e);X.rotate(a);X.scale(1+charge*.08,1-charge*.08);X.shadowColor='#000';X.shadowBlur=7;oval(2,5,29,23,'#bcaec1');X.shadowBlur=0;oval(0,0,28,23,'#f8eff8');X.globalAlpha=.62;oval(-8,-8,14,6,'#fff');X.globalAlpha=1;ink('#d8c8dc',6);for(let q of [-1,1]){X.beginPath();X.moveTo(2,q*14);X.lineTo(-4,q*28);X.stroke();ink('#4b3b51',5);X.beginPath();X.moveTo(-4,q*26);X.lineTo(-8,q*31);X.stroke();ink('#d8c8dc',6)}for(let i=0;i<6;i++){ink(RC[i],4.6);X.beginPath();X.moveTo(-20,-8+i*3);X.quadraticCurveTo(-55,-38+i*10,-47,-6+i*5);X.stroke()}txt('♥',-3,10,12,'#ff6f9f','center');X.restore();X.save();X.translate(P.x+e*34,P.y+(H-12-P.y)*e);X.rotate(a);let u=Math.sin(t*5)*.015;X.scale(1+u,1-u);X.shadowColor='#000';X.shadowBlur=7;oval(-3,4,29,20,'#bcaec1');X.shadowBlur=0;oval(-5,0,29,20,'#fff8ff');oval(15,0,19,14,'#f7eaf5');X.globalAlpha=.55;oval(3,-7,16,6,'#fff');X.globalAlpha=1;X.fillStyle='#fff5ff';for(let q of [-1,1]){X.beginPath();X.moveTo(-9,q*14);X.lineTo(-18,q*29);X.lineTo(2,q*20);X.fill()}for(let i=0;i<5;i++){ink(RC[(i+2)%6],4.5);X.beginPath();X.moveTo(-18,-14+i*7);X.quadraticCurveTo(-36,-29+i*11,-30,-3+i*8);X.stroke()}for(let q of [-1,1]){oval(8,q*7,4,3,'#21162b');oval(9,q*6,1.2,1.2,'#fff')}oval(27,-3,1.8,1.4,'#6a536b');X.shadowColor=g?'#ffe681':'transparent';X.shadowBlur=g?12:0;X.fillStyle='#ffe681';X.beginPath();X.moveTo(18,-5);X.lineTo(41,0);X.lineTo(18,5);X.fill();X.shadowBlur=0;ink('#bfa143',1.5);for(let i=0;i<3;i++){X.beginPath();X.moveTo(23+i*5,-3);X.lineTo(25+i*5,3);X.stroke()}X.restore();X.lineCap='butt'}"""
cob="""function cob(e){X.save();X.translate(e.x,e.y);let j=e.hit?-.08:Math.sin(t*3+e.x)*.02,boss=e.type==3,a=boss?-Math.PI/2:(e.type==1?(e.state==1?e.a:Math.atan2(e.vy,e.vx)):e.type==6?Math.atan2(e.vy,e.vx):Math.atan2(A.y-e.y,A.x-e.x)),r=e.r,k=e.type==4?4:e.type==6?6:e.type==5?5:e.type,arm=k==5||k==6,hus=arm?'#5f6537':'#315f36',corn=e.hit?'#fff':k==4?'#8050ae':arm?'#c08c2c':'#e5b53d';X.globalAlpha=.25;oval(4,r*.52,r*.72,r*.2,'#000');X.globalAlpha=1;X.scale(1+j,1-j);X.rotate(a);if(boss&&wave==5){X.fillStyle='#253f27';for(let q of [-1,1]){X.save();X.scale(1,q);X.beginPath();X.moveTo(-r*.2,5);X.quadraticCurveTo(-r*1.5,r*1.1,-r*1.25,r*.15);X.lineTo(-r*.55,-3);X.fill();X.restore()}}X.fillStyle='#244b2d';for(let q of [-1,1]){X.save();X.scale(1,q);X.beginPath();X.moveTo(-r*.05,2);X.quadraticCurveTo(-r*.78,r*.88,-r*1.1,r*.36);X.lineTo(-r*.56,0);X.fill();X.restore()}X.fillStyle=hus;for(let q of [-1,1]){X.save();X.scale(1,q);X.beginPath();X.moveTo(-r*.08,1);X.quadraticCurveTo(-r*.58,r*.62,-r*.92,r*.3);X.lineTo(-r*.5,0);X.fill();X.restore()}oval(2,3,r*.9,r*.57,arm?'#775d25':'#9b6e21');oval(0,0,r*.87,r*.54,corn);X.globalAlpha=.22;oval(-r*.2,-r*.16,r*.4,r*.14,'#fff');X.globalAlpha=1;for(let i=-2;i<3;i++)for(let q=-1;q<2;q++)oval(i*r*.25,q*r*.27,r*.12,r*.1,k==4?RC[(i+q+8)%6]:e.hit?'#fff':((i+q)&1?'#ffd85a':'#e6aa30'));for(let q of [-1,1]){oval(r*.45,q*r*.16,3,2.5,'#191712');oval(r*.46,q*r*.15,1,1,'#fff')}ink('#191712',2);X.beginPath();X.moveTo(r*.22,-r*.29);X.lineTo(r*.56,-r*.22);X.moveTo(r*.22,r*.29);X.lineTo(r*.56,r*.22);X.stroke();if(boss&&wave!=MAX){X.fillStyle='#ffd85a';X.beginPath();X.moveTo(-r*.2,-r*.58);X.lineTo(-r*.05,-r);X.lineTo(r*.16,-r*.62);X.lineTo(r*.38,-r*.92);X.lineTo(r*.48,-r*.45);X.closePath();X.fill()}if(boss&&wave==5){oval(0,-r*.68,r*.1,r*.08,'#d5544e');ink('#e9c76b',3);X.beginPath();X.arc(0,0,r*1.18,Math.PI*.65,Math.PI*1.35);X.stroke()}if(boss&&wave==9){ink('#c3d3cc',2);for(let i=0;i<4;i++){let q=i*T/4;X.strokeRect(Math.cos(q)*r*.7-5,Math.sin(q)*r*.7-5,10,10)}X.beginPath();X.arc(0,0,r*.76,0,T);X.stroke()}if(boss&&wave==MAX&&!e.a){ink('#8f62a3',5);for(let i=0;i<6;i++){let q=(i-2.5)*.3;X.beginPath();X.moveTo(-r*.55,0);X.quadraticCurveTo(-r*(1.15+Math.abs(q)),q*r*2,-r*1.55,q*r*2.5);X.stroke()}oval(-r*.2,0,r*.23,r*.23,'#100814');ink('#c294c5',2);X.beginPath();X.arc(-r*.2,0,r*.34,0,T);X.stroke()}if(e.type==1&&e.state==1){ink('#ff704f',2);X.beginPath();X.moveTo(r*.7,0);X.lineTo(220,0);X.stroke()}if(arm){ink('#e2d9a0',2.5);X.beginPath();X.ellipse(0,0,r*.96,r*.64,0,0,T);X.stroke();for(let i=-1;i<2;i++){X.beginPath();X.moveTo(-r*.68,i*r*.24);X.lineTo(r*.64,i*r*.24);X.stroke()}}if(boss){for(let i=0;i<5;i++){X.fillStyle=i<Math.ceil(e.hp/e.max*5)?RC[(e.p+5)%6]:'#292c32';X.fillRect(-30+i*13,r+7,11,5)}if(e.in>0){X.globalAlpha=e.in/1.55;for(let i=0;i<6;i++){ink(RC[i],3);X.beginPath();X.arc(0,0,52+(1-e.in/1.55)*70+i*5,0,T);X.stroke()}X.globalAlpha=1}}else if(e.type>0){X.fillStyle='#13201c';X.fillRect(-r,-r-10,r*2,4);X.fillStyle='#fff';X.fillRect(-r,-r-10,r*2*e.hp/e.max,4)}X.restore()}"""
block="""function block(o){if(o.d&&!bp)return;let w=o.d&&bp==1,q=w?bt/2:1;X.globalAlpha=w?.15+.3*q:1;X.fillStyle='#17170f';X.fillRect(o.x+5,o.y+6,o.w,o.h);X.fillStyle=w?'#b9a13c':'#48462d';X.fillRect(o.x,o.y,o.w,o.h);X.globalAlpha*=.45;X.fillStyle='#d6cf82';X.fillRect(o.x+3,o.y+3,o.w-6,3);X.fillRect(o.x+3,o.y+3,3,o.h-6);X.globalAlpha=w?.15+.3*q:1;ink(w?(bt>1.3?'#ff7968':'#ffe767'):'#88855a',w?4:2);X.strokeRect(o.x,o.y,o.w,o.h);if(w){X.globalAlpha=1;X.fillStyle=bt>1.3?'#ff7968':'#ffe767';X.fillRect(o.x,o.y+o.h-6,o.w*q,6);ink('#fff4a5',1);for(let i=12;i<o.w;i+=24){X.beginPath();X.moveTo(o.x+i,o.y);X.lineTo(o.x+i-16,o.y+o.h);X.stroke()}if(A.x+24>o.x&&A.x-24<o.x+o.w&&A.y+24>o.y&&A.y-24<o.y+o.h)txt('MOVE!',o.x+o.w/2,o.y+o.h/2+5,13,'#fff','center')}X.globalAlpha=1}"""
world="""function worldArt(l){X.save();let b=wave<5?0:wave<9?1:wave<12?2:3,h=H*.74,z=cl((l-.28)/.72,0,1),sw=Math.sin(t*.35);X.fillStyle=['#111915','#1b110d','#10191b','#100b17'][b];X.globalAlpha=.5;X.fillRect(0,h-45,W,90);if(b==0){for(let d=0;d<2;d++){X.globalAlpha=d?.7:.22;ink(d?'#17271d':'#304137',d?5:3);for(let i=0;i<(d?13:18);i++){let x=i*(d?82:58)+(d?17:0),y=h+20+(i%3)*10,s=sw*(d?5:2);X.beginPath();X.moveTo(x,H);X.quadraticCurveTo(x-9+s,y-62,x+3+s,y-112-(i%4)*17);X.stroke();if(d)for(let q of [-1,1]){X.beginPath();X.moveTo(x,y-47);X.quadraticCurveTo(x+q*22,y-67,x+q*31,y-52);X.stroke()}}}X.globalAlpha=.18;ink('#788177',9);for(let i=0;i<3;i++){X.beginPath();X.arc(W/2,h+72,230+i*18,Math.PI*1.08,Math.PI*1.92);X.stroke()}}else if(b==1){X.globalAlpha=.78;for(let i=0;i<7;i++){let x=45+i*145,w=50+(i&1)*10,top=150+(i%3)*25;X.fillStyle='#090707';X.fillRect(x+8,top+8,w,h-top+28);X.fillStyle='#24140f';X.fillRect(x,top,w,h-top+28);X.fillStyle='#3c2418';X.fillRect(x+4,top+5,8,h-top+18);X.beginPath();X.moveTo(x,top);X.lineTo(x+w*.35,top-28-(i&1)*18);X.lineTo(x+w,top);X.fill()}X.globalAlpha=.28;ink('#c1995b',10);X.beginPath();X.arc(W/2,h+55,320,Math.PI*1.08,Math.PI*1.92);X.stroke()}else if(b==2){X.globalAlpha=.6;ink('#3e4b4d',8);for(let x=55;x<W;x+=135){X.beginPath();X.moveTo(x,H);X.lineTo(x+45,115);X.lineTo(x+110,H);X.moveTo(x+22,350);X.lineTo(x+85,240);X.stroke()}ink('#74817f',2);for(let y=170;y<h;y+=72){X.beginPath();X.moveTo(25,y);X.lineTo(W-25,y+20);X.stroke()}X.globalAlpha=.2;for(let i=0;i<4;i++){X.strokeRect(115+i*220,150+(i&1)*70,135,235);X.strokeRect(128+i*220,163+(i&1)*70,109,209)}}else{X.globalAlpha=.72;for(let i=0;i<9;i++){let left=i<5,x=left?i*92:W-(i-4)*92,sg=left?1:-1,y=h-120-(i%4)*43;X.fillStyle=i&1?'#17101d':'#09070c';X.beginPath();X.moveTo(x,H);X.lineTo(x+sg*(34+(i%3)*26),y);X.lineTo(x+sg*(72+(i%2)*30),H);X.fill();ink('#44334e',2);X.beginPath();X.moveTo(x+sg*5,H);X.lineTo(x+sg*(34+(i%3)*26),y);X.stroke()}X.globalAlpha=.22;ink('#85648d',7);for(let i=0;i<5;i++){X.beginPath();X.moveTo(i&1?0:W,100+i*82);X.quadraticCurveTo(W/2+Math.sin(t*.2+i)*45,50+i*78,W/2+(i-2)*32,h-8);X.stroke()}}if(z){X.globalAlpha=.16+z*.32;ink(RC[(wave+1)%6],2+z*3);X.beginPath();X.moveTo(0,h);for(let i=0;i<9;i++)X.lineTo(i*120,h-12-Math.sin(i*1.8+wave)*18*z);X.stroke();if(b==0)for(let i=0;i<3;i++){X.beginPath();X.arc(W/2,h+72,230+i*18,Math.PI*1.1,Math.PI*(1.1+.8*z));X.stroke()}if(b==2)for(let i=0;i<4;i++){let x=190+i*185;X.beginPath();X.moveTo(x,h);X.lineTo(x+35,160);X.stroke()}}X.restore()}"""
bossart="""function bossArt(l){if(!queen)return;X.save();let z=.12+l*.17,cx=W/2,cy=315;X.globalAlpha=z;if(queen==3){ink('#d45b54',4);for(let i=0;i<4;i++){X.beginPath();X.arc(cx,cy,105+i*58,.2+i*.12,T-.35);X.stroke()}ink('#879087',3);for(let i=0;i<8;i++){let a=i*T/8+t*.03;X.beginPath();X.moveTo(cx+Math.cos(a)*80,cy+Math.sin(a)*60);X.lineTo(cx+Math.cos(a+.12)*430,cy+Math.sin(a+.12)*280);X.stroke()}ink('#755383',6);for(let i=0;i<5;i++){X.beginPath();X.moveTo(i&1?0:W,150+i*70);X.quadraticCurveTo(cx,80+i*55,cx+(i-2)*28,cy);X.stroke()}}else if(wave==5){X.globalAlpha=z*.75;oval(cx,cy,155,155,'#2d170d');X.globalAlpha=z;ink('#d2a656',10);X.beginPath();X.arc(cx,340,285,Math.PI*1.08,Math.PI*1.92);X.stroke();ink('#f0c86b',3);for(let i=0;i<7;i++){let a=Math.PI*1.1+i*.13;X.beginPath();X.moveTo(cx+Math.cos(a)*180,cy+Math.sin(a)*150);X.lineTo(cx+Math.cos(a)*300,cy+Math.sin(a)*250);X.stroke()}}else if(wave==9){ink('#8ea09a',2);for(let r=70;r<300;r+=55){X.beginPath();X.arc(cx,cy,r,t*.08,t*.08+T);X.stroke()}for(let i=0;i<12;i++){let a=i*T/12-t*.04;X.beginPath();X.moveTo(cx+Math.cos(a)*55,cy+Math.sin(a)*42);X.lineTo(cx+Math.cos(a)*390,cy+Math.sin(a)*265);X.stroke()}X.globalAlpha=z*.45;for(let i=0;i<4;i++)X.strokeRect(90+i*220,150+(i&1)*80,130,190)}else if(wave==13){ink('#805783',9);for(let i=0;i<9;i++){let a=i*T/9,x=i&1?0:W,y=95+i*58;X.beginPath();X.moveTo(x,y);X.quadraticCurveTo(cx+Math.cos(a+t*.15)*190,105+Math.sin(a)*95,cx,285);X.stroke()}X.globalAlpha=.34+l*.2;oval(cx,285,90+Math.sin(t)*7,90+Math.sin(t)*7,'#010203');ink('#ad83aa',3);X.beginPath();X.arc(cx,285,112+Math.sin(t*2)*5,0,T);X.stroke()}X.restore()}"""

s=subf(s,'body','uni',body)
s=subf(s,'uni','cob',uni)
s=subf(s,'cob','block',cob)
s=subf(s,'block','worldArt',block)
s=subf(s,'worldArt','bossArt',world)
s=subf(s,'bossArt','chroma',bossart)

pat=r"B\.forEach\(b=>\{.*?\}\);Q\.forEach"
rep="""B.forEach(b=>{let a=Math.atan2(b.vy,b.vx);X.save();X.translate(b.x,b.y);X.rotate(a);if(b.r>7){X.fillStyle='#8dffe3';X.beginPath();X.moveTo(13,0);X.lineTo(-10,6);X.lineTo(-19,0);X.lineTo(-10,-6);X.fill();X.globalAlpha=.3;X.fillRect(-32,-2,24,4)}else{let c=b.team==1?'#8dffe3':'#e9b83e';oval(0,0,b.r+2,b.r*.7,c);X.globalAlpha=.5;oval(-2,-1,b.r*.45,b.r*.2,'#fff');X.globalAlpha=1;ink(b.team==1?'#dffff8':'#8d641c',1);X.beginPath();X.moveTo(-b.r,0);X.lineTo(b.r,0);X.stroke()}X.restore();X.globalAlpha=1});Q.forEach"""
s,n=re.subn(pat,rep,s,count=1,flags=re.S)
if n!=1: raise SystemExit('projectile replacement failed')
p.write_text(s)

p=Path('src/04-ui-input.js');u=p.read_text()
old="""X.globalAlpha=.7;X.fillStyle='#bbb7be';X.beginPath();X.ellipse(305,365,30,25,0,0,T);X.fill();X.beginPath();X.ellipse(655,335,35,22,-.1,0,T);X.fill();X.fillStyle='#766f58';X.beginPath();X.moveTo(678,331);X.lineTo(717,335);X.lineTo(678,339);X.fill();"""
new="""X.globalAlpha=.72;oval(307,371,31,23,'#827e86');oval(299,363,15,7,'#aaa5ad');ink('#77727a',4);for(let i=0;i<4;i++){X.beginPath();X.moveTo(282,360+i*4);X.quadraticCurveTo(252,338+i*8,257,368+i*4);X.stroke()}oval(650,339,31,19,'#918c95');oval(671,339,17,13,'#aaa5ae');X.fillStyle='#77717a';for(let q of [-1,1]){X.beginPath();X.moveTo(643,326+q*11);X.lineTo(634,315+q*18);X.lineTo(651,321+q*13);X.fill()}X.fillStyle='#766f58';X.beginPath();X.moveTo(681,334);X.lineTo(718,339);X.lineTo(681,344);X.fill();"""
if old not in u: raise SystemExit('intro corpse seam missing')
u=u.replace(old,new,1)
p.write_text(u)

p=Path('scripts/test.mjs');t=p.read_text();needle="if(!h.includes('createBiquadFilter')";t=t.replace(needle,"if(!h.includes('shadowBlur')||!h.includes('system-ui')||!h.includes('function oval'))throw Error('v0.24 dimensional renderer missing');"+needle,1);p.write_text(t)
