{
  "name": "stretchicorn",
  "version": "0.21.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && python3 scripts/package.py && npm run check:size",
    "check:size": "node scripts/check-size.mjs",
    "test": "npm run build && node scripts/test.mjs",
    "smoke": "npm run build && node scripts/release-smoke.mjs",
    "verify": "npm test && node scripts/release-smoke.mjs && python3 scripts/package.py && npm run check:size",
    "wavedash:build": "npm run build && node scripts/build-wavedash.mjs",
    "wavedash:dev": "npm run wavedash:build && wavedash dev",
    "wavedash:push": "npm run wavedash:build && wavedash build push"
  }
}
